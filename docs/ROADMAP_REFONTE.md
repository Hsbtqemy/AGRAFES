# Roadmap « Refonte » — canvas UI + capacités corpus

> Statut : **plan consolidé — à dérouler par tranches**. Date : 2026-06-30.
> Consolide trois notes de design : [`DESIGN_prep_text_canvas.md`](DESIGN_prep_text_canvas.md) (refonte UI, **colonne vertébrale**), [`DESIGN_peritext_conventions.md`](DESIGN_peritext_conventions.md) (+ [grounding](DESIGN_peritext_conventions_grounding.md)) (segmentation 2-grain & alignement), [`DESIGN_metadata_templates_filtering.md`](DESIGN_metadata_templates_filtering.md) (métadonnées).
> Complète, sans la remplacer, [`ROADMAP.md`](ROADMAP.md) (tracker moteur/gouvernance, historique livré). Ici = le **plan produit/UX à venir**.
>
> **Avancement (2026-07-01).** **R1 et R2 livrés** ; **R3.1 + R3.2 livrés** (branche `refonte`, PR #195). R3.2 = aligneur hiérarchique 2 étages `length_bounded` (DP `gale_church.py`), migration 022 `bead_id`, contrat 1.6.36, exclusion collision same-bead, front (option + marqueur de bead). Prochain = **R3.3** (méthode par lot + éditeur de beads ; l'option de stratégie et la provenance `explain` sont déjà là). Queues : R2.2 *contrôles front* = **sans objet** (voie A, approfondi le 2026-07-01) ; R2.3 *cas blob 2-grains* = **note de design écrite** ([`DESIGN_R2_3_blob_two_grain.md`](DESIGN_R2_3_blob_two_grain.md)), moteur/import à coder. L'**éditeur de beads manuel** (R3.3) reste volontairement différé.
>
> **Avancement (2026-07-07).** **R4·R5·R6.1·R6.2·R6.3 livrés** → la colonne vertébrale R1→R6.3 est complète (contrat **1.6.49**, migrations **001–025**). R6.3 = métadonnées spécifiques par type (`meta_json.fields`, datalist + templates + champs libres). **Reste R6.4** (tiroir Avancé · responsive · retrait legacy).
> **Constat de parité (bloquant pour le retrait) :** le canvas **n'est pas à parité** avec `CurationView`/`AnnotationView` — voir [`DESIGN_R6_4_canvas_parity.md`](DESIGN_R6_4_canvas_parity.md) (24 écarts qualifiés). Le **retrait legacy** (T4b) régresserait ~24 features → **différé et renuméroté R6.5 (post-parité)** ; R6.4 se limite au responsive (T4a, sûr) + éventuel tiroir Avancé (T3, relogement).
> **Besoins réels exprimés (2026-07-07) — le déclencheur « cas réel » est atteint**, donc repriorisation devant le R6.4 cosmétique :
> - **Éditeur inline de curation** (override manuel du texte curé) — ✅ **relogé au canvas (2026-07-19)** : éditeur inline par-ligne dans `CurationPane`, staged en `manual_overrides → /curate` (α), couvre override d'une suggestion **et** édition directe d'une unité non-suggérée. Note : [`DESIGN_curation_inline_edit_canvas.md`](DESIGN_curation_inline_edit_canvas.md). **Solde le gap parité #9 + l'écart « édition directe du texte » R5.3.**
> - **Alignement ancré-source — modèle de fond** (R3.3 élargi) — **modèle conceptuel figé** dans [`DESIGN_source_anchored_alignment.md`](DESIGN_source_anchored_alignment.md) (moyeu = original/parent surchargeable · **deux formes** du corpus documentaire/alignée · alignement = **couche additive non-destructive**, matrice = projection dérivée jamais figée · gestes **couper/fusionner/grouper/ré-ancrer** chirurgicaux · export = **matrice multilingue** = le CSV prototype `Test-Alignement(Hugo)`). **D8/D9 tranchés (2026-07-07)** : ajouts = ligne en flux `[ajout]` (+ section à part en option) sur l'axe `unit_status` déjà bâti ; coupes = offsets `target_char_start/end` `nullable` sur `alignment_links` (verbatim, non-destructif), fusion = bead 1-M sans schéma. **Ticket-ready.** Mécaniques wire/bead dans [`DESIGN_alignment_curation_model.md`](DESIGN_alignment_curation_model.md), **ancré sur un cas réel** (`LeCléziotest.db` : FR 8 phrases ↔ EN 7, un 2-1 non-déclarable + un mislink pivot orphelinant FR3). **Livrés (CI verte)** : socle K3 `bead_uid` (mig 026) · affichage bead en bloc (`05cb44b`) · socle « couper » (offsets cible `target_char_start/end`, mig 027, contrat 1.6.50, `8f2bc1f`) · audit expose `target_text_raw`+offsets (1.6.51, `f80348e`) · rendu d'un bead coupé en tranches 1-1 · **cut-picker** (« ✂ Couper » sur un 2-1, clic entre mots → `set_target_span` ×2). · **export matrice moteur** (`POST /export/matrix`, 1.6.52, projection coupes+concat+omission — vérifié sur Le Clézio FR/EN/RO) · **C2 déclencheur front** (carte « Matrice multilingue » de l'écran Exports : sélecteur famille = moyeu + format CSV/TSV → export 1 clic). **Reste** : fusionner/grouper · cut-picker N-1 + vue famille (cf. note §9). Trois trous structurels : **(A)** liens *ancrés au pivot* (pas de ré-ancrage), **(B)** verbe *grouper en bead* absent — `add-target` fabrique un 1-2 fantôme re-signalé en collision, **(C)** gestes de réparation éclatés Segmentation↔Alignement. Cause racine : `run_id` porte à la fois provenance **et** identité de bead. **Décision structurante D1 = K3** (colonne `bead_uid` indépendante du `run_id`, migration 026 additive + backfill byte-identique, bascule des 3 sites de collision) → débloque le group **inter-runs** (le MVP intra-run du §4 ne couvre PAS le cas réel). Tranches : socle K3 → group/ungroup (action additive `set_bead`/`clear_bead`) → re-anchor → renvoi contextuel. **[MIXTE : moteur + front]** ; D8/D9 tranchés (offsets cible + `unit_status`).
> **Correction :** le **merge/cut** de resegmentation **existe déjà au canvas** (SegmentPane, vue Brut, ⇧⇩✂ — R5.4b-3) ; seules les **ops de structure** (insert/delete/zone/propagate) + le **matcher inter-doc** restent legacy (R5.4d différé, garde l'écran Segmentation en vie).
> **À vérifier / auditer (ajouté 2026-07-07) — modes d'import & reconnaissance des types de document.** Passer en revue les **importeurs** (`importers/` : DOCX lignes/¶, TXT, TEI, ODT lignes/¶, CoNLL-U ; dispatch unique `dispatch.py`) et la **détection partagée** (`importDetect` = format + langue, source unique import local **et** ShareDocs ; `familyDetect` = lien source↔traduction) pour confirmer leur robustesse et couvrir les cas limites. Évaluer l'**auto-détection du `resource_type`** (R6.3, aujourd'hui saisi à la main) à l'import. **[MIXTE : moteur + front].** **MAJ 2026-07-20 : audit livré** → [`AUDIT_IMPORT_2026-07-20.md`](AUDIT_IMPORT_2026-07-20.md) (15 findings `IMP-01…15`, suivi dans [`AUDIT_FOLLOW_UP.md`](AUDIT_FOLLOW_UP.md)) ; **verdict `resource_type` = garder manuel** (signal nul, non auto-détectable) ; **IMP-01 critique** (hang/OOM `_analyze_external_ids`) en tête de file P0.

## 1. Principe directeur

- **Colonne vertébrale = le canvas.** Chaque **couche** est une tranche verticale *capacité moteur → surface UI*. Alignement et Métadonnées restent des **vues à part** (déjà plat / objet différent).
- **Front-pur d'abord** (bas risque) → moteur/destructif ensuite, **toujours prototypé sur une WORKCOPY** (jamais le corpus réel — la resegmentation supprime l'alignement, ADR-017).
- **Frontière moteur ↔ front gérée explicitement** (cf. §4) : chaque item est tagué et porte ses implications contrat/migration. C'est le garde-fou de risque n°1.
- **Numérotation unifiée.** Les labels locaux qui se télescopaient (canvas `T0-T4`, segmentation `A0-A3`, péritexte `T1-T6`, métadonnées `M1-M3`) sont remappés en phases **R1…R6** (table de correspondance §5).

## 2. Carte des couches

| Couche / vue | État | Capacité moteur requise | Dominante |
|---|---|---|---|
| **Rôles** | ✅ dans le canvas | — (CRUD conventions déjà là) | front |
| **Segmentation 2-grain** | ✅ persist + dérivé + ¶ (R2) | parent persist · établir grossier · stats/doc | mixte |
| **Alignement** (vue séparée) | ✅ par-clé/position | garde-fous · aligneur longueurs (par-algo) | moteur |
| **Curation** (surimpression) | ❌ couche | — (`lib/curation*` déjà extraits) | front |
| **Annotation** (tokens) | ❌ couche | — (spaCy déjà câblé ; friction amont) | front |
| **Concordancier** (tauri-app) | ❌ rôle+valeur | query `JOIN unit_roles` | mixte |
| **Métadonnées** (écran) | 🟡 plat | notes · tags · templates | mixte |

## 3. Séquence R1 → R6

Légende des tags : **[FRONT]** = TypeScript/Tauri, contrat inchangé · **[MOTEUR]** = Python engine/sidecar · **[MIXTE]** = les deux. Tout `[MOTEUR]`/`[MIXTE]` touchant un endpoint déclenche la **discipline contrat** (§4).

### R1 — Canvas pilotable *(bas risque, front-dominant)*

But : le canvas devient un vrai poste de travail (conscience d'état + Rôles soignés).

- **R1.1** Chip de stade minimal (Brut vs Segmenté · N) — **[FRONT]** ✅ *fait* (dérive `unit_count`).
- **R1.2** Stade complet : grossier/fin · parent ¶ · aligné — **[MOTEUR]** *(lecture seule)* : exposer des **stats par doc** (`has_external_id`, `has_parent`, `n_aligned`, `max_len`). Endpoint read-only → **contrat** (§4). Logique en `services/`. ✅ *fait* — `GET /documents/stats` (`documents_service.document_stats`, contrat 1.6.34).
- **R1.3** Rendu des chips enrichis + bandeau d'état permanent — **[FRONT]** (étend `TextCanvasView._renderStateStrip`). ✅ *fait*.
- **R1.4** Polish couche Rôles dans le canvas — **[FRONT]**. ✅ *fait* (couche Rôles opérationnelle dans le canvas).

> Stat de R1.2 est **partagée avec R2** (visualisation hiérarchique) → la livrer une fois, l'exploiter deux fois.

### R2 — Segmentation 2-grain *(le nouveau modèle ; WORKCOPY)*

But : matérialiser la hiérarchie **paragraphe ⊃ phrase** (représentation = pointeur parent `meta_json.parent_*`, sans migration).

- **R2.1** Persister le parent à la segmentation fine — **[MOTEUR]** : `resegment_document` remplit `meta_json.parent_*` au lieu de `None` ([grounding §3](DESIGN_peritext_conventions_grounding.md), `segmenter.py:535`). **Pas de migration, pas de contrat** (écriture interne) ; undo déjà couvert. ✅ *fait* — `segmenter.py:544`/`:328` écrivent `{"parent_n": …}` (clé *logique* : `parent_n` seul, **pas** `parent_unit_id`, l'unité source étant supprimée).
- **R2.2** Établir le grain grossier depuis **indices pluggables** (séparateurs présents `¤`, lignes vides, rôles structurels, re-lecture `source_path`) — **[MIXTE]** : dérivation moteur (réutiliser `/segment/structure_sections` avant de coder neuf) + contrôles front. Endpoint éventuel → contrat. ✅ *fait (voie A)* — `coarse_grain.derive_coarse_blocks` (pur, hors sidecar.py). **Décision figée** : sections ≠ paragraphes → intertitres/structure *classés*, jamais fusionnés (le seul indice qui regroupe est `parent_n`) ; `¤` = intra-¶ (bloc composite, `fine_count`). **Contrôles front : SANS OBJET** (approfondi 2026-07-01) — la voie A a effondré l'idée d'indices concurrents : le seul signal qui *regroupe* est `parent_n`, les autres ne font que classer/marquer. Rien à piloter. Micro-écart noté (non traité) : `coarse_grain.STRUCTURAL_ROLES` code en dur `{"intertitre"}` au lieu de lire `unit_roles.category='structure'` (cosmétique, classification d'en-tête seulement). La re-lecture `source_path` bascule dans la queue R2.3 (cas blob).
- **R2.3** Visualisation hiérarchique (phrases groupées sous ¶) + cas blob 2-grains — **[FRONT]** (consomme le parent exposé en R1.2). ✅ *fait* — lib pure `coarseGrain` (miroir moteur) + regroupement ¶ dans RolesPane. **Correction vs plan** : le parent n'est pas lu via `structure_sections` mais exposé comme champ `parent_n` sur `GET /units` (**contrat 1.6.35** — additif, read-only). **Différé** : le *cas blob 2-grains* — **note de design** [`DESIGN_R2_3_blob_two_grain.md`](DESIGN_R2_3_blob_two_grain.md) (c'est du moteur/import, pas du front : un blob n'a pas de 2-grain à afficher, il faut le *produire* — split ¶ en amont du split phrase, `text_source` en base puis `source_path` en repli).

### R3 — Alignement à la phrase *(livrable contrastif ; dépend R2)*

But : des liens phrase↔phrase fiables, par-clé **ou** par-algorithme.

- **R3.1** Garde-fous anti-dérive — **[MOTEUR]**. ✅ *fait* — post-check `_check_anchor_consistency` (`qa_report.py`, gate `anchor_drift`). Le *pré-vol* a fondu en **avertissement post-alignement** porté par l'étage ¶ de R3.2 (décision C — l'aligneur hiérarchique absorbe l'écart de cardinalité).
- **R3.2** Aligneur par longueurs borné par ancre + dispatch — **[MIXTE]**. ✅ *fait* — aligneur **hiérarchique 2 étages** `align_by_length_bounded` (DP pure `gale_church.py`) ; beads N-M persistés (**migration 022 `bead_id`**) ; dispatch `length_bounded` (`_run_alignment_strategy` + CLI, **contrat 1.6.36**) ; exclusion collision same-bead ; front (option `<select>` + marqueur de bead). **Dépend de R2.1** (ancre `parent_n`).
- **R3.3** UX choix de méthode par texte/lot + affichage de la provenance d'un lien — **[FRONT]** (coexistence auto/manuel déjà câblée : `protected_pairs`). ✅ *fait* — sélecteur de stratégie **partagé paire ⇄ famille** (donc « méthode par lot » = le run famille lit le même `#align-strategy-sel`) ; provenance (`explain`) ; **parité du marqueur de bead** dans la vue famille multi-colonnes (`prep-fam-link--bead`) ; **avertissements advisory de l'aligneur** (D4 « X % de ¶ non appariés », D7 « grain ¶ ») désormais **remontés dans le résumé de run paire** (ils l'étaient déjà côté famille). Pur front, zéro contrat/migration. **Seul différé : l'éditeur de beads manuel** (fusion/scission au clic — note de design dédiée [`DESIGN_R3_3_bead_editor.md`](DESIGN_R3_3_bead_editor.md) ; hors périmètre R3.3, les données restent fidèles sans lui, ticket conditionné à un cas de curage réel). **MAJ 2026-07-07 : cas réel exprimé → ticket à ouvrir (cf. Avancement 2026-07-07).**

- **R3.4** Matrice = surface d'alignement primaire + périmètre validation/export — **[FRONT] + [MOTEUR léger]** *(cadrage autoritatif : [`DESIGN_alignment_parity_tranche6.md`](DESIGN_alignment_parity_tranche6.md) ; ancrage amont : [`DESIGN_upstream_anchoring.md`](DESIGN_upstream_anchoring.md))*. 🟢 *substantiel livré — endgame dépriorisé restant* — **T6.1** (matrice primaire « Alignement », ex-`AlignPanel` → « Révision fine » secondaire, commit `9f1aaf0`) + **T6.2** (handoff scopé `🔎` cellule → Révision fine pré-chargée paire+lien, `AlignPanel.scopeTo`, commit `0e9b3f1`) **livrés + revus adverse**. **Décision de périmètre (2026-07-18, vérifiée au code — note §8)** : la matrice suffit au structurel (créer/couper/fusionner/rattacher/**✕ supprimer**/aligner) ; la validation n'est *requise* que parce que l'**export TEI ne garde par défaut que `status='accepted'`** (`exporters/tei.py`, `tei_package.py`) alors que l'**aligneur crée des liens `NULL`=non-révisé** (`aligner.py:298`) et que l'UI Prep **ne passe aucun `status_filter`** (`ExportsScreen.ts`) → **export d'alignement vide** sur une famille non validée (piège vivant). **Fix = 1 défaut, pas un module (D-P7)** : export `["accepted"]` → **« tout sauf rejeté »** + `status_filter` en option → le **rejet** (déjà matrice, ✕) redevient le seul acte qui retire ; « accepté » = provenance spécialiste optionnelle. **« accepté » gardé (D-P11)** : *load-bearing* via `preserve_accepted` (protège du recalcul, `sidecar.py:339-357`) → pas mort ; identité recentrée « épinglé/vérifié/survit au ré-alignement », scope microscope, non exigé. **Suivi doc/famille = tableau DÉRIVÉ (D-P9, sort de pause ; cadrage : [`DESIGN_corpus_progress_rollup.md`](DESIGN_corpus_progress_rollup.md))** : pas raviver `workflow_status` (mort/pris par segmentation) mais **agréger les signaux vivants** (couverture + acceptés + collisions) par doc/famille dans Documents (étendre `familyView` + vue hiérarchie de `MetadataScreen`), avec **deep-links** vers matrice/Révision fine (`scopeTo`) — calculé, jamais périmé, respecte « capacités indépendantes » ; agrégat moteur additif (`/families` non schématisé → freeze nul), séquencé après D-P7. **« Révision fine » → microscope optionnel (D-P10)** : accept/reject fin spécialiste + collisions + qualité + orphelins + source-modifiée ; **reléguer acquis (T6.1) + renommé « Contrôle » ✅ 2026-07-19**. **Parcolab filtrer-vs-annoter (D-P12) : DIFFÉRÉ** — le TEI filtre mais n'annote pas le statut (`tei.py:552`) ; trancher quand on saura si Parcolab lit un `@cert` ou traite chaque `<link>` comme vérité. **Livrés (2026-07-18/19)** : **D-P7** export « non rejeté » forme 3 (`d90139c`) · **D-P9** rollup dérivé moteur+front, deep-links famille, vue hiérarchie (`ae609d2`→`b9231f1` + fix rognage QA `95882bd`, **QA shell OK sur corpus réel**) · **D-P10** renommage « Contrôle ». **Décidés, zéro build** : D-P8 (pas de validation matrice), D-P11 (garder « accepté »). **Reste = endgame dépriorisé** : T6.2b (post-run `run_id`/étape-suivante sur matrice) · T6.3 (retrait redondant) · T6.4 (run paire → famille-only) ; D-P12 (Parcolab) différé.

### R4 — Conventions / péritexte propres

But : axe statut + lift des marqueurs + concordancier qui affiche les rôles.

- **R4.1** `units.unit_status` (`non_traduit`/`ajout`) — **[MOTEUR]** + **[FRONT]**. ✅ *fait* — **migration 023** ; `units_service.set_unit_status`/`bulk` + routes dédiées `/units/set_status` (+bulk, contrat **1.6.37**) ; `unit_status` exposé par GET /units ; **filtre `query`** (FTS + regex + CLI `--unit-status`, contrat **1.6.38**) ; **filtre concordancier** (`#filter-unitstatus-sel` + chip + history). Axe **orthogonal au rôle**, enum validé en service (escape-hatch). Décisions : [`DESIGN_R4_1_unit_status.md`](DESIGN_R4_1_unit_status.md). Découplé de R4.2 (lift) / R4.3 (affichage) ; **pas** d'auto-pont orphelin→statut (populé par R4.2 ou curation).
- **R4.2** Lift marqueurs `[T]/[Ch]/[InterT]/[non traduit]/[+]` → rôle/statut (passe post-import idempotente) — **[MIXTE]**. ✅ *fait* — **moteur** `marker_lift.py` (hors `sidecar.py`, growth-gate) : `parse_markers` (allowlist stricte, casse insensible, marqueurs *trailing* seulement → gloses `[…]` mid-texte préservées) + `lift_document_markers` (dry-run défaut, remplit rôle/statut **si NULL** → préserve le manuel + signale conflits, nettoie `text_norm`, garde `text_raw`, réindexe FTS ; idempotent via « marqueur encore dans `text_norm` ») ; CLI `lift-markers` ; **`POST /lift/markers`** (dry-run+apply, **contrat 1.6.39**) ; **front** = bouton « Lifter les marqueurs » + **aperçu dry-run** (synthèse + conflits + diff par unité + apply) dans `RolesPane` (donc onglet Rôles **et** TextCanvasView), helpers purs `lib/markerLift.ts` + client `liftMarkers`. Peuple l'axe R4.1 → rend le filtre concordancier démontrable. Décisions : [`DESIGN_R4_2_marker_lift.md`](DESIGN_R4_2_marker_lift.md). Zéro migration.
- **R4.3** Concordancier affiche rôle + statut — **[MIXTE léger]**. ✅ *fait* — **moteur** : `unit_role`/`unit_status` ajoutés aux hits `/query` (2 SELECT page FTS+regex + `_build_hits_core` segment/kwic) **et** aux unités alignées (`_fetch_aligned_units`, forward + siblings). **Contrat 1.6.40** version-only : la forme du hit est volontairement opaque (`hits: {type:object}`) → aucun changement de schéma, la freeze ne se déclenche pas. **Front** `tauri-app` : `results.ts` `appendUnitBadges` (badges rôle+statut sur pivot **et** alignés, les 2 renderers) + helpers purs `ui/roleStatus.ts` (labels **sans catalogue** : concordancier découplé) + CSS `.hit-badge`. Décisions : [`DESIGN_R4_3_role_status_display.md`](DESIGN_R4_3_role_status_display.md). Zéro migration. `token_query` (CQL) différé.

### R5 — Couches Curation + Annotation *(front-dominant ; la refonte UI ressentie)*

But : ramener curation et annotation **dans le canvas**, en surimpression.

- **R5.1** Couche **Curation** surimpression légère (marqueur discret + diff à la demande + toggle global) — **[FRONT]**. ✅ *fait* — base partagée `CanvasUnitList` extraite de RolesPane (a) ; mode Curation au canvas : dock presets + `curatePreview` (`limit_examples` exhaustif) → **marqueur discret** (b) ; **diff à la demande** (bascule par unité) + **toggle global** (c) ; **Apply** `/curate` + confirm destructif + gestion « éditions non appliquées » (d). Réutilise les ~16 `lib/curation*` + `/curate/preview` + `/curate` ; **contrat inchangé, zéro migration**. Décisions : [`DESIGN_R5_1_curation_layer.md`](DESIGN_R5_1_curation_layer.md). Différé : réindex FTS post-apply reste manuel. **MAJ 2026-07-19 : l'override/édition inline de curation est ✅ relogé au canvas** (éditeur inline `CurationPane`, `manual_overrides → /curate`, note [`DESIGN_curation_inline_edit_canvas.md`](DESIGN_curation_inline_edit_canvas.md)) — solde le gap parité #9.
- **R5.2** Couche **Annotation** tokens (prose colorée par défaut, interlinéaire à la demande) + friction amont (modèles spaCy non bloquants) — **[FRONT] + petite greffe moteur (modèles)**. ✅ *fait* — coloriage UPOS extrait en `ui/annotationProse.ts` (**a**) ; couche Annotation read-only au canvas (`AnnotationPane`, prose colorée) (**b**) ; friction relogée au dock (job+poll `runJobWithPolling`, bande modèle) **+ modèles spaCy Lots 1/3/4** : `source` tri-état *bundled/downloaded/absent*, catalogue étendu, modèle **actif par corpus** (`corpus_info.meta_json`, **`POST /models/active`**, contrats 1.6.42→1.6.44) (**c**) ; **éditeur de token à la demande** au clic (Mot/Lemme/UPOS/XPOS/Feats/Misc) (**d**) ; **vue Étendue** (grille interlinéaire mot/UPOS/lemme) au choix, en **relogeant** le builder du legacy `AnnotationView` (**e**). Notes : [`DESIGN_spacy_models_dual_dist_and_selection.md`](DESIGN_spacy_models_dual_dist_and_selection.md) · [`DESIGN_R5_2c_annotation_friction_and_models.md`](DESIGN_R5_2c_annotation_friction_and_models.md) · [`DESIGN_R5_2e_extended_interlinear_canvas.md`](DESIGN_R5_2e_extended_interlinear_canvas.md). Différé : **Lot 2** (installeur léger/full, ticket build/CI) ; lien « Gérer les modèles » (nav niveau app).
- **R5.3** **Feuille d'actions contextuelle** — les box d'action (éditeur de token, borne/rôle) ne sont plus ancrées aux extrémités du scroll (aller-retours) mais dans une **feuille non-modale fixée au bas de la fenêtre**, partagée par les couches ; padding dynamique du scroll (`ResizeObserver`) pour ne pas masquer la fin du texte ; liseré sur le token en cours d'édition — **[FRONT]**. ✅ *fait* (Annotation + Rôles). Note : [`DESIGN_R5_3_contextual_action_dock.md`](DESIGN_R5_3_contextual_action_dock.md). Différé : Curation (panneau de contrôle global, pas une box par-position). **MAJ 2026-07-19 : l'édition directe du texte est ✅ relogée** via l'éditeur inline `CurationPane` ([`DESIGN_curation_inline_edit_canvas.md`](DESIGN_curation_inline_edit_canvas.md)).
- **R5.4** Couche **Segmentation** au canvas = **segmentation configurable** — **[MIXTE]** *(R5.4a + b-1/b-2/b-3 + c livrés ; R5.4d structure matcher différé)*. Surface `Brut | Phrases | Balises [N] | Personnalisé | Tours` (**Brut** = état courant ; Phrases/Balises dominants ; **Personnalisé** = terminateurs cumulables + Mots + majuscule-après + abréviations libres ; **Tours** = regroupe en tours de parole, grain grossier *ascendant*) ; sous le capot, **un seul mécanisme** : une frontière = *terminateur en flux* (`.!?`, espace) **ou** *motif de ligne* (`—`/`NOM :` = **tours de parole**, saut de ligne = vers, ligne vide = ¶, `[N]`). **Recadrage vérifié** : « pack/langue » ≈ fiction (règle de découpage `_SPLIT_RE` **unique**, la langue ne bascule que 3 mini-jeux d'abréviations) → le bon modèle pour un outil d'analyse = « définis ta segmentation ». Le `_SPLIT_RE` fixe devient un **`SegmentSpec`** paramétrable (params additifs `/segment(/preview)` → **contrat, zéro migration** : `meta_json`). Aperçu en contexte (`/segment/preview`, 0 écriture) → apply (`/segment`) → reload. **Garde-fou = confirm conditionnel** (seulement si un alignement existe) ; **pas de WORKCOPY imposée** (c'est le travail *constitutif* de la DB). Notes : [`DESIGN_R5_4_segmentation_layer.md`](DESIGN_R5_4_segmentation_layer.md) (modèle) · [`DESIGN_R5_4B_segment_canvas_layer.md`](DESIGN_R5_4B_segment_canvas_layer.md) (front). **Différé** : alignement des nouveaux segments (tours/mots ; unité vs token), 3ᵉ niveau, structure matcher (inter-doc → reste legacy, R5.4d). Tranches : **R5.4a moteur `SegmentSpec` ✅** → **R5.4b-1 canvas Brut/Phrases/Balises ✅** (+ moteur : Phrases sans condition de majuscule + ponctuation fermante) → **R5.4b-2 Personnalisé ✅** → **R5.4b-3 anomalies + merge/split ✅** (segments courts + ponctuation orpheline langue-aware → règle le `»` orphelin) → **R5.4c Tours ✅** (regroupe grossier *ascendant*, non destructif : relabel `parent_n`, contrat 1.6.46) → R5.4d structure matcher (différé).

### R6 — Métadonnées + finitions UI

But : enrichir les notices et **retirer le legacy** une fois la parité atteinte.

- **R6.1** ✅ `documents.notes` (notes-to-self) + relibeller la note de relation — **[MIXTE]** : **migration 024** + `_UPDATABLE` → contrat 1.6.47 ; front = champ MetadataScreen. *(non indexé FTS ; ≠ `doc_relations.note`)*.
- **R6.2** ✅ Labels filtrables (`document_tags` N-N namespacé `kind/value`) + CRUD + filtre `tags` — **[MIXTE]** : **migration 025** + endpoints `services/tags_service` → contrat 1.6.48 ; front = picker (Prep) + filtre (concordancier, sémantique OR).
- **R6.3** ✅ Templates par `resource_type` (datalist libre + `TYPE_TEMPLATES` front + champs libres hybrides, persistance `meta_json.fields`) — **[MIXTE]** : front-dominant + `meta` fusionné dans `meta_json` (contrat 1.6.49, sans migration).
- **R6.4** Tiroir « Avancé » (exceptions/history/diag) · responsive container-query — **[FRONT]** *(cadrage : [`DESIGN_R6_4_canvas_parity.md`](DESIGN_R6_4_canvas_parity.md))*. **Correction (R5.4)** : retirer **Cur/Annot** (relogés en R5.1/R5.2) ; **garder l'écran Segmentation** tant que le structure matcher (inter-doc) n'est pas relogé (R5.4d, différé).
  > **Cadrage figé (2026-07-20, vérifié au code — note §7).** Re-vérif : les 5 🔴 restants (#9 relogé depuis) = **4 chantiers, TOUS front-only** (zéro contrat/migration : #1/#10 réutilisent `/curate*` déjà live, #2/#3/#4/#22 = in-memory + localStorage). Couplage : #2+#4+#14 indivisibles (`_curateExamples[].status`). **Voie tranchée = parité puis retrait, séquencée incrémentale (option §5.3)** : `R6.4·T4a` responsive ✅ *(livré 2026-07-20)* → **R6.5-A retrait Annotation** (port #22, petit, prouve le playbook) → **R6.5-B parité Curation** (chantiers Revue #2+#4+#14 · Règles #1 · Exceptions #10 · tiroir Avancé T3 pour les 🟠) → **R6.5-C retrait Curation**. 🟡 abandonnés (#13/#15/#17/#18/#24 + la *télémétrie* de #19 ; **mais le bouton undo Apply de curation est rattaché à R6.5-B**, pas abandonné — `prepUndo` déjà câblé au canvas), 🟢 actés design (#11/#12/#16/#21). Le **retrait est renuméroté R6.5** (post-parité). Blast radius allégé par dérive (`curationFocusDoc` déjà mort, clé `prep-curation-doc` réassignée à Rôles) et **confiné à `tauri-prep`**.
- **R6.5** *(nouveau — post-parité)* **Retrait des écrans legacy** `CurationView`/`AnnotationView` — **[FRONT]**. Séquencé A (Annotation) → B (parité Curation) → C (retrait Curation) ; cf. note §7.3. **Garder l'écran Segmentation** (structure matcher R5.4d différé).

## 4. Moteur vs front — la frontière, gérée

**Répartition par phase** (dominante) :

| Phase | Front | Moteur | Contrat ? | Migration ? | WORKCOPY ? |
|---|---|---|---|---|---|
| R1 ✅ | R1.1·R1.3·R1.4 | R1.2 (read) | oui (read-only, 1.6.34) | non | non |
| R2 ✅ | R2.3 | R2.1·R2.2 | oui (R2.3 : `parent_n` /units, 1.6.35) | non | **oui** |
| R3 ✅ | éditeur de beads (différé) | R3.1·R3.2·R3.3 ✅ | oui (1.6.36) | **022 (bead_id)** | **oui** |
| R4 ✅ | R4.1 ✅ · R4.2 ✅ · R4.3 ✅ | R4.1 ✅ · R4.2 ✅ · R4.3 ✅ | oui (R4.1 : 1.6.37/38 · R4.2 : 1.6.39 · R4.3 : 1.6.40) | **023 (R4.1)** | oui |
| R5 🟡 | R5.1·R5.2·R5.3 ✅ · **R5.4b-1/b-2/b-3 ✅ · R5.4c Tours ✅** (Segmentation canvas ; R5.4d matcher différé) | R5.2 modèles · **R5.4a `SegmentSpec` · R5.4b Phrases · R5.4c coarse ✅** | oui (**1.6.45/1.6.46** ; additif) | non (`meta_json`) | non |
| R6 🟡 | **R6.1·R6.2·R6.3 ✅** · R6.4 (T4a responsive + T3 tiroir) · **R6.5 retrait legacy (front-only, cadré §7)** | **R6.1·R6.2·R6.3 ✅** | oui (1.6.47/48/49) ; **R6.4/R6.5 : aucun (front pur)** | **024·025** | non |

**Lecture** : R1 et R5 sont **front-dominants** (itération rapide, risque bas) ; R3 et R4 sont **moteur-dominants** (discipline contrat, migration, tests WORKCOPY) ; R2 et R6 sont mixtes.

**Discipline moteur** (rappel, à appliquer dès qu'un endpoint bouge) :

- **Contrat = 3 artefacts** : `sidecar_contract.py` → `scripts/export_openapi.py` → commit `docs/openapi.json` **+** `tests/snapshots/openapi_paths.json` **+** `docs/SIDECAR_API_CONTRACT.md` (sinon `test_contract_docs_sync` casse ; le snapshot ne fait que *warn* sur un ajout).
- **Write-path** : ajouter la route au predicate `_WRITE_PATHS` + prendre `with self._lock()`.
- **Growth-gate** : `sidecar.py` ne doit pas croître de > 500 l. nettes/90 j → **logique en `services/`**, handlers fins (modèle A-01). `marker_lift`, l'aligneur, les checks QA vivent **hors** `sidecar.py`.
- **Migrations** : fichier `NNN_*.sql` neuf (dernière = 021), jamais éditer un appliqué.
- **CI** : rejouer le scope exact avant commit — `ruff check src tests` (pas juste `src`) + pytest pertinent.

**Front** : ESLint + `no-unsanitized` (sink `safeHtml`), CSS namespacée (`prep-*`/`app-*`/`shell-*`), pas de dialogue natif (`modalConfirm`/`inlineConfirm`), build = `tsc + vite` + vitest.

## 5. Correspondance des anciens labels → R

| Ancien | Source | Devient |
|---|---|---|
| canvas T0 (coquille + Rôles) | prep_text_canvas | **R1** (socle, fait) |
| segmentation A0 (stade) | prep_text_canvas §10 | **R1.1** (fait) |
| segmentation A1 (parent) **=** péritexte « parent persist » | doublon | **R2.1** (un seul item, fait) |
| segmentation A2/A3 | prep_text_canvas §10 | **R2.2 / R2.3** (faits ; blob 2-grains différé) |
| péritexte T4 (garde-fous) / T6 (aligneur) | peritext | **R3.1 / R3.2** (faits) |
| péritexte T1 (statut) / T2 (lift) / T3 (concordancier) | peritext | **R4.1 / R4.2 / R4.3** |
| canvas T1 (curation) / T2 (annotation) | prep_text_canvas §7 | **R5.1 / R5.2** |
| métadonnées M1 / M2 / M3 | metadata | **R6.1 / R6.2 / R6.3** |
| canvas T3 (tiroir) / T4 (responsive+retrait) | prep_text_canvas §7 | **R6.4** |

## 6. Jalons de valeur

- **R2** = le double grain *réel* dans les données (hiérarchie ¶ ⊃ phrase persistée).
- **R3** = l'alignement phrase automatique borné = **le cœur contrastif**.
- **R1 + R5** = la refonte UI *ressentie* (canvas pilotable + curation/annotation en couches).
