---
chantier: U-03
statut: interrompu
audit: docs/AUDIT_2026-06-12.md
---

# U-03 — couverture de test de `tauri-app`

**Arrêté sur** — point d'arrêt après `dom.ts`, 4 tranches, `tauri-app` 81 → 128 tests, commit `46dad80`, 29 juin 2026.

## Reste

- [ ] `features/metaPanel.ts` — non couvert (rendu + I/O sidecar) ; demanderait un render-smoke avec mocks
- [ ] `features/importFlow.ts` — non couvert, même raison

## Contexte

Statut au tracker : **🟦 partiel (point d'arrêt)**. Tout l'extractible à logique pure est
testé, sur quatre tranches (PR #179→#183) : `features/stats.ts` (rendu compare et parseur
de filtres), `ui/results.ts` (rendu KWIC et parallèle, dont l'**anti-XSS du highlight**
prouvé RED-sur-cassé), `features/docSelector.ts` (persistance localStorage et checklist),
`ui/dom.ts` (`escapeHtml` comme primitive anti-XSS, et `elt`). Plus `search.ts` (#60) en
amont. `tauri-app` passe de 81 à 128 tests, gatés en CI, happy-dom.

Le reste est du rendu et de l'I/O sidecar : faible retour sur investissement en test
unitaire, le tracker le qualifie de « ticket dédié si besoin ». C'est un arrêt décidé, pas
un abandon — mais le code n'est pas couvert, d'où cette fiche.

**FE-06 est le même chantier sous un autre code.** U-03 est le finding de l'audit du
12 juin ; FE-06 (audit du 28 juin, P2-18) est la campagne qui l'a rempli — le tracker écrit
littéralement « (= U-03) » sur la ligne FE-06, et « cf. FE-06 » sur celle d'U-03. Ils
partagent le même reliquat. Pas de fiche `FE-06.md` : elle dupliquerait celle-ci et ferait
apparaître deux chantiers ouverts pour un seul travail. Le dernier commit de FE-06 étant un
`docs:`, il se rendra clos depuis git seul, ce qui est correct.

Ne pas confondre avec T-05, qui porte le manque de **driver** E2E (Playwright ou
WebDriver) ; U-03 porte la couverture unitaire de `tauri-app`.

Sources : `docs/AUDIT_FOLLOW_UP.md` lignes U-03 et FE-06, corps de `46dad80`.
