---
chantier: R2
statut: interrompu
---

# R2 — segmentation deux grains (paragraphe ⊃ phrase)

**Arrêté sur** — passe `qa/import-sauts-de-ligne.md` close **23/23** sur le binaire du
26 août ; correctif d'import et micro-écart `STRUCTURAL_ROLES` livrés, RED prouvé sur
l'ancien code dans les deux cas. Reste le réimport des 15 documents, à vous.

## Reste

- [ ] Réimporter les 15 documents « blob » de la base de travail (bitextes GRAFE) — les
      sources sont toujours sur le disque. **Il faut supprimer les documents d'abord** :
      `assert_not_duplicate_import` bloque sur `source_hash` et aucun forçage n'existe. Coût
      mesuré de la suppression : rien de curé à la main (0 tag, 0 note, 0 rôle, 0 métadonnée,
      0 `text_start_n`), 24 902 tokens et 2 liens d'alignement calculés sur un blob donc sans
      valeur — mais **13 `doc_relations`** (familles source↔traduction) sont supprimées
      explicitement par `delete_documents` et seront à rétablir (le `familyDetect` de P6 les
      recrée peut-être à l'import par lot : à vérifier). Chemin de suppression audité : toutes
      les tables référentes cascadent (`tokens`, `alignment_cell_statuses`, `document_tags`,
      `prep_action_history`), et le service rollback en cas d'échec
- [ ] **Un import qui ne produit aucune unité indexable doit le dire.** Trois producteurs
      mesurés le 26 août, aucun signalé : un DOCX à sauts de ligne doux importé en mode
      paragraphes reste un blob ; un DOCX sans marqueur `[n]` importé en mode numéroté sort
      tout en `structure` (déjà noté le 25 août, `qa/italique-import.md`) ; et un ODT en liste
      **automatique** fait de même alors que l'écran affiche une numérotation (voir l'item
      dédié plus bas). Le plus frappant est que **le moteur calcule déjà les deux signaux et
      ne les lit pas** : `qa_report._check_import_integrity` filtre sur `unit_type='line'`,
      donc sur un document 100 % `structure` il ne trouve ni trou ni doublon ni unité vide et
      conclut `severity: "ok"` — tout en renvoyant `line_unit_count: 0` dans la même charge
      utile ; et `LONG_LINE_THRESHOLD = 2000` range bien l'unité de 110 000 caractères dans
      `long_unit_ext_ids`, sans que les unités longues relèvent la moindre sévérité. Troisième
      manque : ce rapport est un **produit d'export** (`ExportsScreen`, « Rapport QA corpus »),
      jamais montré après un import. Reco : une sévérité sur `line_unit_count == 0` (ce n'est
      pas une heuristique — zéro unité indexable n'est jamais un import voulu), le compte
      d'unités longues en avertissement, et le rapport rendu visible à la fin de l'import
- [x] R2.2 — micro-écart `STRUCTURAL_ROLES` : **fait le 26 août**. `structural_roles_for(conn)`
      lit `unit_roles.category='structure'` et devient la source unique — les quatre sites qui
      ont un `conn` la prennent (dérivation `coarse_blocks_for_doc`, aligneur, bascule ¶,
      numérotation ¶ de la matrice), et `sidecar.py` abandonne sa requête recopiée. Le
      constant reste le défaut de la fonction *pure* et le repli si le catalogue est illisible
      (base antérieure aux migrations 013/018). **Pas cosmétique, contrairement à la note** :
      dans la base de travail, `titre` (5 unités), `epigraphe` (3), `dedicace` (1) et le rôle
      **custom `Infos_AGRAFES`** (1) sont déclarés `structure` et n'étaient reconnus par aucun
      de ces quatre chemins — 10 unités que la route `propagate` traitait déjà, elle, comme
      des murs de section. Les deux chemins se contredisaient
- [ ] `conventions_service.create` range un rôle en `category='text'` par défaut sans
      consulter `STRUCTURE_ROLE_NAMES` (l. 111) : un rôle nommé `intertitre` créé par l'API
      sans catégorie explicite n'est **pas** structurel, alors que la migration 018 l'aurait
      rangé là. Sans conséquence sur la base de travail (tout y est déjà `structure`), mais
      c'est la seule façon de fabriquer aujourd'hui un corpus incohérent — à trancher :
      défaut déduit du nom, ou laissé au choix explicite
- [ ] Le filet d'avertissement à l'import a un **troisième** cas, mesuré le 26 août et le plus
      trompeur des trois : `Asimov-Foundation_FR_réaligné.odt` est **une seule liste
      automatique** (1 `<text:list>`, 1141 `<text:list-item>`, 0 marqueur `[n]`). LibreOffice
      y affiche une numérotation de 1 à 1141 que le style de liste **calcule au rendu** et qui
      n'existe nulle part dans le texte. L'utilisateur voit un document numéroté, choisit le
      mode qui porte ce mot, et obtient **1141 unités hors index** sans un mot. Le bon mode est
      *paragraphes*, qui reconstruit la même numérotation en positionnel (`external_id`
      1 → 1141) et rend l'alignement par ancre FR/EN parfait (1141 identifiants communs, 0
      orpheline). Les quatre `.odt` du dossier `GRAFE-Lit-EnFr-REAligné-DOCX` sont dans ce cas
- [ ] R2.3 — cas « source disparue » : re-produire le grain ¶ d'un blob déjà en base depuis
      `text_source`. **Sans cas d'usage vivant** : aucun des 15 blobs n'en relève. À ne coder
      que si un blob se présente sans son fichier source ; la priorité D2 de la note est alors
      à revoir (0 ligne vide dans les cas réels)

## QA

- qa/import-sauts-de-ligne.md

## Contexte

R2.1 (persistance du parent, `meta_json.parent_n`), R2.2 (`coarse_grain.derive_coarse_blocks`)
et R2.3 (visualisation hiérarchique, contrat 1.6.35) sont livrés depuis le 1er juillet.

**Le prérequis bloquant du cas blob est tombé le 26 août — dans l'autre sens que prévu.** Le
§0 de la note posait qu'aucun mode d'import ne produit de blob, donc que coder le split serait
spéculatif. Mesure sur `corpus_agrafes.WORKCOPY.db` : **15 documents sur 58** tiennent en une
seule unité de 68 000 à 110 000 caractères. Les DOCX sources contiennent **un seul `<w:p>`**
portant 853 à 1304 **`<w:br/>`** ; python-docx rend `<w:br/>` en `\n` *dans* `Run.text`, donc
`docx_numbered_lines`, qui prend `<w:p>` pour clef, ne voyait qu'un paragraphe. Deux formes, les deux
observées en base : paragraphe commençant par `[n]` → le `re.DOTALL` laissait `.+` avaler tout
le fichier en une unité `line` (11 documents) ; sinon aucun match → une unité `structure`,
donc **hors FTS** (4 documents). `odt_numbered_lines` portait la même regex, et
`text:line-break` rend aussi `\n`.

Le correctif est donc **à l'import**, pas dans une resegmentation : le paragraphe est découpé
sur ses sauts de ligne doux *avant* le match `[n]`. Le découpage se fait au niveau des segments
(`rich_text.para_to_rich_lines` / `odt_para_to_rich_lines`), ce qui garde les `<hi>` équilibrés
quand un style enjambe une coupure ; le `DOTALL` est retiré des deux regex. Les modes
`*_paragraphs` sont laissés tels quels — « un `<w:p>` = une unité » est leur contrat (ADR-012).

Réserve à ne pas oublier : sur ces bitextes, les segments délimités par `\n` font 85 à 107
caractères en moyenne. C'est déjà le grain fin, pas un grain ¶ — l'enjeu n'était pas « deux
grains » mais « un grain, correct ».

Décision figée de R2.2, inchangée : sections ≠ paragraphes. Les intertitres et unités de
structure sont *classés*, jamais fusionnés — le seul indice qui **regroupe** est `parent_n`.

Note : `docs/DESIGN_R2_3_blob_two_grain.md` (§0 réécrit avec les mesures du 26 août).

Sources : `docs/ROADMAP_REFONTE.md` §3 R2.2/R2.3, corps de `4e01f79`, mesures du 26 août 2026.
