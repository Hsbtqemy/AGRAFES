---
chantier: R2
statut: clos
---

# R2 — segmentation deux grains (paragraphe ⊃ phrase)

**Arrêté sur** — la passe QA-06 close le 1er septembre 2026 : « Pré-remplir » d'abord borné au
texte (`aadf69c`), puis doté de son garde-fou conditionnel et de son aperçu à blanc — un seul
mécanisme pour les deux, comme la mesure de la veille l'avait proposé. Le chantier de fond était
clos depuis le 26 août (correctif d'import des sauts de ligne doux, micro-écart
`STRUCTURAL_ROLES`, défaut de catégorie de `conventions_service.create`, passe
`qa/import-sauts-de-ligne.md` close **23/23**, 15 documents « blob » réimportés). RED prouvé sur
l'ancien code pour chacun des correctifs. **Aucun reliquat de code.**

Le dernier item, le relevé du grain ¶, a été clos le même jour — non pas fait, mais **dissous
par la mesure** : le grain ¶ ne manque pas, il se décide au mode d'import, et la chaîne
`docx_paragraphs` → `segment` le produit (vérifié sur un fichier réel, 11 paragraphes de 3,94
phrases). Ce corpus n'en a pas parce qu'il a été importé en lignes numérotées. Les deux
constats trouvés en le vérifiant sont partis dans `IMPO-01` et `R3`.

## Reste

- [x] **Le grain ¶ n'existe presque nulle part — et c'est normal, il n'a jamais été
      travaillé.** Mesuré le 26 août : 6 documents sur 58 portent un `parent_n` (6 019
      unités). **Aucun import n'en crée** — `docx_numbered_lines` n'écrit que `sep_count`
      dans `meta_json` — donc tout `parent_n` vient d'un geste humain ou d'une segmentation
      fine. Origines retrouvées : `Beigbeder-Francs_FR` porte **24 gestes ¶ enregistrés**
      (10 valeurs de `parent_n`), `Rowling-Philosopher_EN` **1 geste** (2 valeurs) — ce sont
      des **paragraphes de travail**, pas des données abîmées ; `LeClezio-Chercheur` EN/FR a
      une vraie segmentation fine (`external_id` tombés, 875 ¶ pour 932 unités, 1,07 phrase
      par ¶). Restent `Asimov-Foundation_EN` (`parent_n = 1` sur ses 1304 unités) et
      `Beigbeder-Francs_EN` (1:1), posés avant que l'historique des actions n'existe : origine
      inconnue, mais **pas** une resegmentation en phrases — leurs `external_id` ont survécu,
      ce que celle-là détruit. **Le critère ne vaut que pour ce chemin** (relu le 1er
      septembre) : `resegment_document_markers` écrit les `external_id` *depuis* les marqueurs
      `[N]` et pose `parent_n` — il laisse donc exactement cette trace ; ne pas s'en servir
      pour diagnostiquer un autre document. **Correction d'un diagnostic erroné** : j'avais écrit « grain ¶
      faux, séquelle du défaut d'import » et proposé de réimporter trois documents ; c'était
      faux, rien n'est à réparer. Ce qui reste vrai et utile à connaître : sur
      `Asimov-Foundation_EN`, `derive_coarse_blocks` rend **un seul bloc de 1304 unités**,
      donc l'aligneur borné de R3.2 n'y a aucun point d'ancrage — à savoir avant de l'aligner
      par longueurs, quelle que soit l'origine de ce `parent_n`.
      **Clos le 1er septembre 2026 : la question était mal posée, et un fichier réel l'a
      montré.** Ce que l'item lisait comme un manque est une conséquence du **mode d'import**,
      pas un trou de R2. Vérifié sur `testparagraphesAgrafes.docx` (18 `<w:p>`, aucun
      `<w:br/>`) dans des bases jetables : en `docx_paragraphs` puis `segment`, 17 paragraphes
      deviennent 67 phrases et la dérivation rend **17 blocs dont 11 paragraphes réels**,
      3,94 phrases par ¶ (max 16) — un document à deux grains, sans rien coder. Le même
      fichier en `docx_numbered_lines` donne 17 unités **toutes `structure`** : resegmenter
      n'a alors rien à lire (`units_input: 0`).
      **Correction d'une conclusion erronée**, écrite le matin même : « aucun importeur
      n'écrit `parent_n`, donc un futur corpus arrivera à zéro grain ¶ ». La lettre est vraie
      — `importers/` n'écrit jamais cette clef — mais la conclusion est fausse : le producteur
      est la **chaîne en deux temps**, et `segmenter.py` le dit à l'endroit exact où il pose
      la clef (« Meaningful when source units are paragraphs; trivial when splitting a single
      blob »). Le grain ¶ se décide au mode d'import, pas dans une capacité qui manquerait.
      **La carte pour R3.2**, rejouée sans écriture (`_load_length_blocks` +
      `gale_church_beads`) sur les 32 paires `translation_of` : **29 à ≤ 0,7 %** de blocs non
      appariés — le palier paragraphe tient sur le corpus. Les 3 qui dépassent le seuil de
      15 % sont les documents au grain ¶ posé à la main pendant des essais : Asimov EN→ES
      99,9 %, EN→FR 99,8 %, Beigbeder FR→EN 98,4 %. **Sur une base de travail, un grain
      dégénéré dit « pas encore travaillé », pas « cassé »** — l'aligneur avertit exactement
      là où un geste est resté à moitié fait, ce qui est le comportement voulu.
      **Deux constats partent ailleurs**, ne dépendant ni de cette base ni de R2 : l'import
      qui rend `status: ok`, `warnings: []` et `units_line: 0` sur un document entièrement
      `structure` → `IMPO-01` ; et le seuil `_PARA_UNPAIRED_WARN` qui rate le document *à
      moitié* paragraphé → `R3`
- [x] **`Pré-remplir` écrivait `parent_n` sur le paratexte, le geste ¶ par segment le
      refusait — corrigé le 1er septembre 2026.** `regroup_document_coarse` lit désormais
      `text_start_n` et borne sa requête (`n >= ?`) comme sa voisine ; les deux chemins du
      même geste s'accordent. L'arbitrage que le §11.9 avait laissé ouvert — « scoper
      changerait le grain de documents existants » — a été tranché **par la mesure** : sur la
      base vive (58 documents), **deux** unités étaient concernées, sur le seul doc 416
      (n=4 et n=33 → `parent_n=1`, ancrées dans le paratexte). La correction est donc
      rétroactivement quasi gratuite : ni migration, ni chemin de réparation, ni artefact de
      contrat (les compteurs changent de valeur, pas de schéma).
      **Correction d'une mesure erronée** : le message du commit `aadf69c` annonce l'inverse
      — « 0 dans la base vive, 2 dans la copie de travail ». J'avais pris
      `corpus_agrafes.db` pour la base de travail ; c'est un reliquat du **30 juin**
      (53 documents, dernière action le 30/06). La base vivante est
      `corpus_agrafes.WORKCOPY.db` (58 documents, dernière action le 31/08 à 12h56). Les deux
      chiffres sont donc à intervertir ; la décision, elle, ne bouge pas — 2 unités sur 1228
      pour ce document. Trois tests, RED prouvé sur
      l'ancien code pour les deux qui portent la borne — dont un qui reproduit exactement le
      résidu du doc 416. **Piège laissé pour la suite** : `coarseRegroup.ts`, qui calculera
      l'aperçu du garde-fou QA-06, n'a aujourd'hui aucun appelant et ne borne rien ; le
      brancher sans lui passer des unités bornées recréerait la divergence aperçu↔apply
      qu'on vient de fermer — **refermé le jour même**, voir l'item suivant : le helper reste
      délibérément non borné, c'est `_coarseScopeUnits()` qui filtre, et un test le tient

- [x] **QA-06 correctifs 2 et 3 — garde-fou et aperçu à blanc avant que « Pré-remplir »
      écrase des frontières manuelles. Fait le 1er septembre 2026.** Venu de R6 le 2026-08-27. Vérifié : les deux
      `modalConfirm` de `SegmentPane.ts` (l. 832 et 1186) gardent la **resegmentation** et la
      **propagation**, pas « Pré-remplir », dont le chemin appelle `regroupCoarse` sans
      confirmation. `coarseRegroup.ts` existe et sait calculer le regroupement côté front
      « avant que POST /segment/coarse ne le persiste » — la matière de l'aperçu est donc
      déjà là, elle n'est pas branchée en garde. L'annulation, elle, est bien livrée
      (`prepUndo.ts`, `set_paragraph` couvre les deux gestes, contrat 1.6.63)
      **Enjeu mesuré le 1er septembre 2026** sur la base vive (`corpus_agrafes.WORKCOPY.db`,
      58 documents) : 24 frontières ¶ posées à la main, 23 sur `Beigbeder-Francs_FR` (doc 416)
      et 1 sur `Rowling-Philosopher_EN` — **aucune** venue de « Pré-remplir ». Un
      « Pré-remplir » (préréglage `tours`) sur le doc 416 changerait aujourd'hui **1228 unités
      dont 1228 déjà ancrées** : les 23 gestes partent en un clic, sans question. Cinq autres
      documents sont dans le même cas (1266, 1254, 932, 929 et 347 unités déjà ancrées).
      **Forme proposée** : un seul mécanisme sert les deux correctifs — calculer le
      regroupement localement avec `coarseRegroup.ts`, comparer aux ancres courantes, et n'ouvrir
      le `modalConfirm` que si des unités déjà ancrées changeraient (même patron conditionnel
      que `needsAlignmentConfirm`, `SegmentPane.ts:831`). Le compte rendu par ce calcul EST
      l'aperçu à blanc. À mettre dans un helper **pur**, donc testable, et à borner sur
      `_textStartN` — le pur TS n'est pas borné, exactement comme son miroir Python, c'est son
      appelant qui borne.
      **Livré, et la forme proposée corrigée sur un point qui change le résultat.** La fiche
      disait « n'ouvrir le `modalConfirm` que si des unités **déjà ancrées** changeraient ».
      Codé tel quel, puis mesuré : c'est la mauvaise grandeur. `Beigbeder-Francs_EN` porte un
      `parent_n` sur ses 1267 unités, **chacun égal au `n` de son unité** — 1267 ancres, et
      **zéro** paragraphe : rien n'y est groupé avec rien. La garde y aurait annoncé « 1266
      segments perdus ». C'est exactement le défaut que `alignmentLossNote` documente déjà
      (annoncer 5 770 liens avant d'en détruire deux) et que l'audit §11.16 nomme.
      **La bonne unité de compte est le paragraphe**, pas l'ancre : un segment seul n'est pas
      un paragraphe — la règle `taille > 1` que le moteur applique déjà deux fois
      (`toggle_paragraph_boundary`, `_blocks_anchored`). Un paragraphe **survit** ssi un
      nouveau bloc a exactement ses membres ; absorbé ou coupé, il est perdu.
      `currentParagraphs` + `previewCoarseRegroup` rendent `paragraphsTotal / paragraphsLost /
      segmentsAffected`, et `coarseRegroupGuard` ne demande que si `paragraphsLost > 0`.
      **Ce que la garde dit maintenant** : « redécouperait ce document en 82 tours · déferait
      8 paragraphes sur 8 — 1226 segments concernés · « Annuler » rend le geste ».
      **Le piège de l'item précédent est refermé** : `coarseRegroup.ts` a un appelant, et reste
      non borné — c'est `_coarseScopeUnits()` qui filtre sur `_textStartN`, comme
      `regroup_document_coarse` borne sa requête et laisse `regroup_by_boundary` libre. Le test
      qui le tient a lui aussi été refait : sa première version passait avec ou sans la borne,
      elle ne prouvait rien.
      Deux autres décisions : les lignes à **rôle structurel restent** dans l'aperçu (le
      basculement ¶ les refuse comme murs de section, « Pré-remplir » les écrit comme les
      autres) ; et un **motif illisible côté JS ne fait pas taire la garde** — `(?P<x>…)`
      compile en Python et pas en JS, donc on retombe sur le seul compte qui ne demande aucune
      regex, les paragraphes en place. Même raisonnement pour une **lecture d'unités ratée** :
      `_units` vide est indiscernable d'un document vide, donc on demande.
      **Mesuré sur la base vive** (`corpus_agrafes.WORKCOPY.db`, 58 documents) en rejouant le
      calcul avec les fonctions du moteur : **5** documents déclencheraient la garde, pas 6 —
      416 `Beigbeder-Francs_FR` 8 ¶ / 1226 segments / 82 tours, 410 `Asimov-Foundation_EN`
      1 ¶ / 1304 / 19, 404 `Rowling-Philosopher_EN` 2 ¶ / 351 / 4, 371 `LeClezio-Chercheur_FR`
      53 ¶ / 112 / 1, 370 `LeClezio-Chercheur_EN` 51 ¶ / 108 / 1. **364 `Beigbeder-Francs_EN`
      sort de la liste** : 0 paragraphe, rien à perdre, la garde s'y tait.
      **Les chiffres du paragraphe précédent sont donc à lire comme un compte d'ancres, pas de
      travail perdu** — deuxième correction de la même mesure en un jour. « 1228 unités dont
      1228 déjà ancrées » se lit « 8 paragraphes, 1226 segments » ; les cinq nombres suivants
      (1266, 1254, 932, 929, 347) étaient déjà des ancres *changées*, pas des ancres totales.
      La décision, elle, n'a jamais bougé : ce geste devait demander.
      **Vérification de l'annulabilité au code**, pas de mémoire : `_handle_segment_coarse`
      passe bien `record_action=_recorder`, qui écrit une action `set_paragraph` et ses
      snapshots avant l'écriture — le message a le droit de promettre « Annuler ».
      22 tests (17 sur le helper pur, 5 sur la surface), RED prouvé sur l'ancien
      `SegmentPane.ts` pour les trois qui exigent une modale. 1390 tests verts sur
      `tauri-prep`, `tsc` vert sur `tauri-prep` et `tauri-shell`, eslint propre. **Zéro
      artefact de contrat** : ni la route, ni son corps, ni sa réponse ne bougent — c'est un
      garde-fou de front
- [x] Réimporter les 15 documents « blob » — **fait le 26 août**, en mode lignes numérotées.
      Les 15 comptes attendus sont tombés juste à un près (`Rankin-Naming_EN` : 1814 au lieu
      de 1584, voir ci-dessous). Zéro blob restant dans la base, 58 documents, aucune
      traduction orpheline. Le crible « volume en base vs volume de la source », passé sur les
      58 documents dans la foulée, a trouvé **une** anomalie sans rapport avec ce chantier :
      `Coe-House_ES` ne contenait que **7 % de son texte** (74 unités sur 1119), tronqué entre
      avril et août sans trace dans l'historique des actions — sa source étant intacte, il a
      été réimporté (doc 445, 1119 unités, 4..1122). `Swift-Light-AL_FR` avait aussi perdu son
      rôle et son rattachement : rétablis. 53 documents conformes, 3 ShareDocs non vérifiables
      localement (source WebDAV) mais aux volumes normaux
- [x] **Rankin, décision de corpus (26 août)** : son `EN` reste la variante *anglais-espagnol*
      (1814 unités), donc Rankin s'aligne par ancre côté ES quand les six autres œuvres
      s'alignent côté FR. Ce n'est pas une erreur d'import à réparer : les écarts de
      segmentation viennent de choix d'alignement **manuels** faits dans les fichiers sources,
      qui seront repris dans le programme. Conséquence à connaître : Rankin EN↔FR ne peut pas
      s'aligner par `external_id` et relèvera de l'aligneur par longueurs ou de la matrice
- [x] R2.2 — micro-écart `STRUCTURAL_ROLES` : **fait le 26 août**. `structural_roles_for(conn)`
      lit `unit_roles.category='structure'` et devient la source unique — les quatre sites qui
      ont un `conn` la prennent (dérivation `coarse_blocks_for_doc`, aligneur, bascule ¶,
      numérotation ¶ de la matrice), et `sidecar.py` abandonne sa requête recopiée. Le
      constant reste le défaut de la fonction *pure* et le repli si le catalogue est illisible
      (base antérieure aux migrations 013/018). **Pas cosmétique, contrairement à la note** :
      dans la base de travail, `titre` (5 unités), `epigraphe` (3), `dedicace` (1) et le rôle
      **custom `Infos_AGRAFES`** (1) sont déclarés `structure` et n'étaient reconnus par aucun
      de ces quatre chemins — 10 unités que la route `propagate` traitait déjà, elle, comme
      des murs de section. Les deux chemins se contredisaient
- [x] `conventions_service.create` — **fait le 26 août**. Sans `category` dans la requête, la
      catégorie se déduit du nom via `STRUCTURE_ROLE_NAMES`, exactement comme la migration 018
      l'a rétro-rempli ; une catégorie explicite est toujours honorée, dans les deux sens, et
      une valeur hors enum retombe sur `text` comme avant. **C'est le commit du jour qui avait
      rendu ce trou conséquent** : tant que `STRUCTURAL_ROLES` codait `{"intertitre"}` en dur,
      le cas était rattrapé quelle que soit la catégorie ; depuis que le catalogue fait
      autorité, un `intertitre` créé en `text` n'est plus un mur de section. Portée mesurée
      avant de coder : le trou n'est atteignable que par l'API — l'interface envoie toujours la
      catégorie (le volet Rôles est scindé en deux sections, `RolesPane.ts:568`), le CLI
      n'expose pas la création, et `create_convention` n'a qu'un appelant. Zéro artefact de
      contrat (`category` n'y est pas schématisé). 5 tests, RED prouvé sur les deux qui portent
      la déduction

## QA

- qa/import-sauts-de-ligne.md

## Contexte

R2.1 (persistance du parent, `meta_json.parent_n`), R2.2 (`coarse_grain.derive_coarse_blocks`)
et R2.3 (visualisation hiérarchique, contrat 1.6.35) sont livrés depuis le 1er juillet.

**Le prérequis bloquant du cas blob est tombé le 26 août — dans l'autre sens que prévu.** Le
§0 de la note posait qu'aucun mode d'import ne produit de blob, donc que coder le split serait
spéculatif. Mesure sur `corpus_agrafes.WORKCOPY.db` : **15 documents sur 58** tiennent en une
seule unité de 68 000 à 110 000 caractères. Les DOCX sources contiennent **un seul `<w:p>`**
portant 853 à 1304 **`<w:br/>`** ; python-docx rend `<w:br/>` en `\n` *dans* `Run.text`, donc
`docx_numbered_lines`, qui prend `<w:p>` pour clef, ne voyait qu'un paragraphe. Deux formes, les deux
observées en base : paragraphe commençant par `[n]` → le `re.DOTALL` laissait `.+` avaler tout
le fichier en une unité `line` (11 documents) ; sinon aucun match → une unité `structure`,
donc **hors FTS** (4 documents). `odt_numbered_lines` portait la même regex, et
`text:line-break` rend aussi `\n`.

Le correctif est donc **à l'import**, pas dans une resegmentation : le paragraphe est découpé
sur ses sauts de ligne doux *avant* le match `[n]`. Le découpage se fait au niveau des segments
(`rich_text.para_to_rich_lines` / `odt_para_to_rich_lines`), ce qui garde les `<hi>` équilibrés
quand un style enjambe une coupure ; le `DOTALL` est retiré des deux regex. Les modes
`*_paragraphs` sont laissés tels quels — « un `<w:p>` = une unité » est leur contrat (ADR-012).

Réserve à ne pas oublier : sur ces bitextes, les segments délimités par `\n` font 85 à 107
caractères en moyenne. C'est déjà le grain fin, pas un grain ¶ — l'enjeu n'était pas « deux
grains » mais « un grain, correct ».

Décision figée de R2.2, inchangée : sections ≠ paragraphes. Les intertitres et unités de
structure sont *classés*, jamais fusionnés — le seul indice qui **regroupe** est `parent_n`.

**Les deux suites, hors périmètre R2.** Le filet d'avertissement à l'import — « aucune unité
indexable » et ses trois producteurs mesurés — est suivi dans `pilotage/IMPO-01.md`, avec la
décision d'aperçu comparatif du 26 août. Et la queue R2.3 « blob dont la source a disparu »
reste **différée sans cas d'usage vivant** : aucun des 15 blobs n'en relevait, leurs sources
étaient toutes sur le disque. À ne coder que si un blob se présente sans son fichier ; la
priorité D2 de la note serait alors à revoir, les cas réels ne portant aucune ligne vide.

Note : `docs/DESIGN_R2_3_blob_two_grain.md` (§0 réécrit avec les mesures du 26 août ; son
statut est passé à « partiellement résolu »).

Sources : `docs/ROADMAP_REFONTE.md` §3 R2.2/R2.3, corps de `4e01f79`, mesures du 26 août 2026.
