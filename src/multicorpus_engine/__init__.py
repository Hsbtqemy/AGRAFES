"""
multicorpus_engine — UI-independent Python core engine for multilingual corpus
import, indexing, and querying. Tauri-ready via CLI JSON I/O.

Increment 1: Explorer minimal (init, import DOCX numbered lines, FTS, query).
"""

__version__ = "0.6.1"
