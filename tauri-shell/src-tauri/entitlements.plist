<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    Entitlements pour le bundle AGRAFESShell sous hardened runtime macOS.

    Le sidecar `multicorpus` est un binaire PyInstaller (externalBin) qui charge,
    à l'exécution, un `Python.framework` signé par python.org (Team ID différent
    de celui qui signe l'app). Sans `disable-library-validation`, la library
    validation du hardened runtime refuse le dlopen :
      « code signature ... not valid for use in process: ... different Team IDs »
    → le sidecar meurt avec code=255 avant d'émettre son JSON de démarrage.

    Référencé par tauri.conf.json (bundle.macOS.entitlements) pour le build et par
    .github/workflows/macos-sign-shell.yml pour la signature/notarisation.
  -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
</dict>
</plist>
